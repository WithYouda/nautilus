# 产品概念设计文档：AI 学习管理助手 (EduFlow)

| 文档版本 | 创建日期 | 状态 |
| :--- | :--- | :--- |
| V1.0 | 2026-07-19 | 创意构思阶段 (草稿) |

## 1. 项目愿景

打造一款“思维无边界”的学习管理工具，解决用户“学习计划难坚持”与“AI对话知识难回溯”两大痛点。核心差异化在于引入 **“对话分支/合并”** 机制，让AI辅助学习像写代码一样具备完整的版本控制感，而交互上保持普通聊天软件的简洁。

## 2. 目标用户

- 需要进行系统性自学（如考研、考证、编程学习）的用户。
- 需要将散乱的AI对话整理为知识体系的知识工作者。

## 3. 核心功能架构

### 3.1 基础架构层

- **多端同步**：覆盖 **Windows 桌面端**、**Android 移动端** 及 **Web 网页端**。
- **数据存储**：云端全量存储，本地仅存缓存（采用 WebSocket 实现增量同步）。

### 3.2 学习计划系统

- 目标设定（遵循 SMART 原则）。
- 任务拆解（支持番茄钟/倒计时）。
- 进度看板（日历视图与甘特图）。
- **联动机制**：计划进度自动关联 AI 辅导内容（例如：未完成“高数第三章”，AI 回复时自动优先调取该章节历史对话）。

### 3.3 AI 智能辅导引擎（核心亮点）

#### 3.3.1 交互模式

- **普通对话**：主界面保持传统的线性聊天流，确保沉浸感。
- **分支与合并（Git-like 逻辑）**：
  - **拆出分支**：用户可在侧边栏节点图选中某条历史消息，点击“从此处分支”，继承完整上下文快照开启新对话。
  - **合并分支**：选中两个分支节点，AI 提取双方摘要生成“观点对比卡片”，由用户勾选采纳后，生成合并后的新结论节点写入父级计划。
- **无限画布**：提供放大按钮，进入全局鸟瞰模式，支持拖拽节点、连线重构。

#### 3.3.2 工具执行层（Harness / Agent）

- **定位**：采用 **Human-in-the-loop（人在回路）** 的 Harness 架构。AI 提出执行计划，用户确认后执行，避免全自动 Agent 的不可控风险。
- **核心能力**：
  - 根据对话内容一键生成 **Markdown 层级大纲**。
  - 调用外部工具生成 **思维导图文件（.md / .xmind）**。
  - 支持“定时自动生成复习摘要”与“手动指令触发”两种模式。

## 4. 交互设计方案

### 4.1 界面布局（三栏式）

- **主聊天区（中）**：展示当前焦点路径下的线性对话流。
- **侧边缩略图（右/左）**：展示整个对话状态的极简节点示意图（仅显示标题/摘要前10字）。用户点击节点即可切换当前焦点或创建分支（右键菜单操作）。
- **全屏画布（模态层）**：点击放大按钮后展开，显示节点完整内容，支持拖拽连线进行复杂合并操作。

### 4.2 移动端适配

- 侧边栏在手机上变更为 **“底部抽屉”** 或 **“悬浮球”**，展开后呈现横向流程图。

## 5. 技术选型建议

| 技术层级 | 推荐方案 | 理由 |
| :--- | :--- | :--- |
| **跨平台 UI** | Flutter | 一套代码覆盖 Win/Android，减少维护成本。 |
| **AI 框架（核心）** | **LangGraph** (正式环境) + **CrewAI** (原型验证) | LangGraph 状态管理极强，完美支持复杂分支合并逻辑；初期可用 CrewAI 快速跑通 MVP。 |
| **前端画布** | React Flow (Web) / Room.js | 解决无限画布与协同状态同步问题。 |
| **数据存储** | 向量数据库（如 Milvus）+ 关系型数据库 | 向量库用于存储对话摘要，实现高效的知识点检索与合并冲突检测。 |

## 6. 开发路线规划 (MVP 最小可行产品)

- **第一阶段（验证期）**：仅做 **Web 端**。只实现“普通对话带记忆”、“手动点击生成大纲”和“手动存为分支”三个按钮。
- **第二阶段（核心期）**：引入 LangGraph 实现完整的侧边栏节点图逻辑。
- **第三阶段（扩展期）**：封装桌面端（Flutter）与移动端适配。

## 7. 待确认与待探讨事项

1. **自动 vs 手动**：AI 生成学习文件（如思维导图）的触发方式，目前定为“两者并存”，但初期开发优先做 **手动指令触发**。
2. **合并颗粒度**：初期仅做“知识块级合并”（整段对话凝练为一张知识卡片），不做逐字文本 Diff 合并。
3. **技术犹豫**：目前初步决定使用 **LangGraph**，放弃自研 Harness，以节省开发成本。
