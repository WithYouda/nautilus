# Nautilus 首片最终验收与交接记录

| 项目 | 内容 |
| --- | --- |
| 验收日期 | 2026-09-08，Asia/Shanghai |
| 验收范围 | 实施计划 Task 0-8、PRD V2 首片事实门与证据门工程链路 |
| 结论 | 计划内 Task 0-8 已完成；事实门通过；证据门工程链路通过，产品级整体验收仍有明确未实现项 |
| 数据边界 | 后端测试使用 pytest 临时目录；Playwright 使用 `/tmp/nautilus-playwright.*`；未打开默认 `data/nautilus.sqlite3` |
| 主库状态 | 默认主库仍停留在 `001`-`004`；独立学习库使用 `011`-`016` |

## 1. 总体验收结论

- **Task 0-8：已完成**
- **事实门：通过**
- **证据门：工程链路通过，整体产品验收未完全关闭**
- **硬性护栏：当前测试样本全部通过**
- **产品价值指标：两项无样本，一项通过**
- **默认数据库：未迁移**
- **部署/提交/推送：未执行**

证据门仍不应宣称完整产品验收通过的原因：

1. 全局 Agent 默认最小摘要上下文尚未完整实现。
2. Agent 批准后的实际读取授权尚未实现。
3. 复核卡的完整权限可见范围 UI 尚未完整实现。
4. 真实长期使用样本尚未产生，恢复委托和复核到下一行动指标为无样本。
5. 事件版本升级器尚未实现；当前只支持事件版本 1，不支持静默升级。

这些不是 Task 0-8 的未完成工程任务，而是首片之后的明确后续工作。

## 2. 需求编号状态

| 编号 | 状态 | 证据 |
| --- | --- | --- |
| `INV-001` | 已实现并验证 | 原文不可变、事件分离、版本追加和彻底删除语义由事实/证据/生命周期测试覆盖 |
| `INV-002` | 已实现并验证 | 事件唯一、聚合顺序、幂等、事务投影、事实回放和证据回放测试覆盖 |
| `INV-003` | 部分实现并验证 | 用户隔离、Agent 申请/拒绝/过期和审计已实现；批准后读取授权未实现 |
| `INV-004` | 已实现并验证 | 六类分析失败、保存恢复和幂等重试测试覆盖 |
| `INV-005` | 已实现并验证 | 已审核标准包、无标准降级和标准版本不可变重算测试覆盖 |
| `INV-006` | 已实现并验证 | 候选主张引用、状态派生、复核、批量处理、替代和回访测试覆盖 |
| `INV-007` | 已实现并验证 | 普通删除、恢复、撤回、彻底删除和依赖主张失效测试覆盖 |
| `INV-008` | 已实现并验证 | 隔离库、备份恢复、默认库保护和迁移边界测试覆盖 |
| `INV-009` | 已实现并验证 | 新领域模型独立闭环，未建立旧模型映射或双写 |
| `INV-010` | 部分实现并验证 | Core/Runtime 权限边界已实现；全部角色功能不在首片范围 |
| `CAP-FACT-001` | 已实现并验证 | 新入口、行动、成果、委托、会话、产出、事件和投影已由 API/UI/Playwright 覆盖 |
| `CAP-FACT-002` | 已实现并验证 | 事实投影回放、失败保留和后续写入测试覆盖 |
| `CAP-EVID-001` | 已实现并验证 | 候选主张、状态、复核、批量处理、替代、回访和证据回放已覆盖 |
| `CAP-EVID-002` | 部分实现并验证 | AI 失败恢复、权限申请/拒绝和继续保存已覆盖；批准读取和完整复核卡权限 UI 未实现 |
| `CAP-MEASURE-001` | 已实现并验证 | 指标 API、前端指标卡、无样本标记和硬护栏测试覆盖 |
| `CAP-FUT-001` | 未实现，后续范围 | 不进入首片 |
| `CAP-DEFER-001` | 未实现，暂缓范围 | 不进入首片 |

## 3. 测量结果

### 3.1 产品价值指标

| 指标 | 结果 | 说明 |
| --- | --- | --- |
| 中断后正确恢复委托比例 | 无样本 | 首片未持久化“首次恢复选择”事件，不为凑指标扩张功能 |
| 复核到下一行动完成率 | 无样本 | 复核与下一行动的观察窗口链接未实现，不伪造样本 |
| 证据可追溯率 | 100% | 当前测试主张均可解析产出、事实事件、成果和标准版本 |

### 3.2 硬性护栏

| 指标 | 结果 | 说明 |
| --- | --- | --- |
| 无依据主张率 | 0% | 当前测试主张均通过引用和标准配方校验 |
| AI 失败时产出保存成功率 | 100% | 指定失败场景中产出仍可保存并重试 |
| 事件重建一致性 | 100% | 事实回放与证据回放测试通过；断链场景明确失败 |
| 权限违规次数 | 0 | 正确拒绝的越权尝试不计为违规 |
| 隐私泄露次数 | 0 | 彻底删除后原文和哈希为空，跨身份不可读 |

## 4. 验证命令与结果

```bash
timeout 120s env PYTHONPATH=backend .venv/bin/python -m pytest -q backend/tests
```

结果：**230 passed, 1 warning**

```bash
npm --prefix frontend run build
```

结果：**通过**

```bash
./node_modules/.bin/playwright test
```

结果：**36 passed**

```bash
git diff --check
```

结果：**通过**

## 5. 实际交付范围

- 独立学习库与 `011`-`016` 迁移。
- 事实门 API、前端工作台和 Playwright。
- 已审核 Python 正则标准包。
- Provider 语义分析与确定性检查。
- 六类分析失败与幂等重试。
- 候选主张、状态派生、复核、批量处理、替代和回访。
- 普通删除、恢复、撤回、彻底删除和证据回放。
- 指标 API、前端指标卡和硬护栏测试。
- 隔离测试、备份恢复演练和默认库保护。

## 6. 已知边界与后续建议

1. 默认主库迁移需要用户单独授权。
2. Agent 批准后的读取授权应在下一阶段实现。
3. 全局 Agent 最小摘要上下文应在下一阶段实现。
4. 复核卡权限可见范围应继续补齐。
5. 事件版本升级器应在引入事件版本 2 前实现。
6. 真实使用样本积累后，再评估恢复委托和复核到下一行动指标。
7. 产品级备份、缓存和搜索索引清理策略应在对应基础设施引入时同步实现。

## 7. 交接结论

计划内 Task 0-8 已全部完成并通过当前自动化验证。下一步不应继续在本计划内扩张功能；应先由用户决定是否进入后续阶段，并单独制定后续规格。
