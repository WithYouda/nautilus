# Nautilus 当前开发状态

更新：2026-09-25。这里只保存当前事实与下一项；历史实现过程通过 Git 查询，不再逐轮复制快照。

## 当前结论

作者本人正在真实试用。事实/证据基础、验证可靠性、连续学习、验证回看和题目讨论已实现；真实使用基线（Task 13B）和产品价值验收尚未完成。三入口导航、原计划续学及回答版本/上下文路径切换与纯图标操作栏已启用；本轮仅固化Astra主导、Sol有限委派的开发规则，未开展整体前端重构。提交见 `git log`。

## 已实现

| 范围 | 当前行为 | 主要位置 |
| --- | --- | --- |
| 学习基础 | 目标起步、第一步、委托/会话/产出、事件与同事务投影回放；无标准可保存、不派生能力状态 | `backend/app/core/`、`learning_setup.py` |
| 证据与权限 | 来源由执行边界赋值；已审核正则标准、候选/复核/质疑/删除重算、范围授权、失败恢复 | `evidence.py`、`review.py`、`agent_runtime.py` |
| 首页与导航 | 学习首页、学习计划、学习记录；首页使用正在学习/已保存/已完成/创建及具体动作，能力观察按需展开 | `Workspace.tsx`、`ReturnReviewCard.tsx` |
| 计划与连续学习 | 新目标和原计划续学分别保留归属；卡片启动重放按目标委托区分，切换时暂停旧会话；前端核对任务归属，不匹配时禁止恢复和发送 | `LearningPlans.tsx`、`learning_setup.py`、`continuity.py`、Core |
| 教学与验证 | 每轮携带分步教学规则，理解确认与正式验证分开；自动提示及学习安排默认折叠，刷新可恢复；先保存作答再评估，最后确认委托完成 | `conversations.py`、`AiLearningRoom.tsx`、`FactWorkspace.tsx`、`verification.py` |
| 验证回看 | 本人按提交版本回看原题/作答；每题反馈在前、整体总结在后；已完成委托也可讨论 | `learning_records.py`、`VerificationReview.tsx` |
| 题目讨论 | 即时右侧用户消息，正文与Provider思考实际流式输出，取消/幂等重试/刷新恢复；与学习室共用呈现组件及复制/回答版本/重新生成/分支占位 | `question_discussion.py`、`QuestionDiscussion.tsx`、`LearningRoomLayout.tsx` |
| 来源与删除 | 同委托有界检索；每段独立展开，长预览渐隐；验证清除传播到关联讨论及思考，迟到输出不能复活 | `DiscussionSources.tsx`、迁移028/029 |

旧今日/独立任务/历史计划/日历/计时界面退出，主工作区不再读取旧规划API，未引入双写；历史数据库不清除。右侧 AI 学习伙伴默认折叠，独立学习室为工具入口。首页/计划/记录共用页头和独立呈现样式，方便后续统一改布局。验证次级入口名为“更多操作 → 彻底删除本次验证内容”。

## 当前试用环境

- 一套环境：`tmp/nautilus-trial-20260919/`。`nautilus.sqlite3` 保存普通会话/配置等，`learning.sqlite3` 保存新学习域；后者结构版本为 **029**，不是第29个数据库。主库使用001–010，学习库使用011–029。
- 启动脚本：`bash tmp/nautilus-trial-20260919/run.sh`。API8018/Web5188绑定 `0.0.0.0`；当前 WSL2 地址 **http://172.17.253.105:5188/**，重新启用时用 `hostname -I` 核对。
- 029及修复构建已获用户授权启用。普通启动只检查结构，不自动建库或迁移；不要在版本不匹配时直接重启。
- 构建：`frontend/dist` 的 `index-C7ZScrqE.js` / `index-CBhMZli1.css`。不把测试构建直接覆盖正在使用的 dist。
- 进程号会变化，操作前读取该trial的 `service.pid` 并核对 `/proc` 命令，不从旧文档复制PID。
- 2026-09-24按用户要求删除3份旧trial备份及对应清单/边车，保留已核对哈希的最新029恢复点：`backups/learning-post-upgrade-20260923T163610213396Z.sqlite3`（相对trial）。删除旧的空 `backups/learning/` 目录；旧默认data环境与diagnostic-backups未操作。
- 升级工具目前仍默认升级前后备份。本次只清理存量；取消后置备份、自动保留数量和常设迁移授权仍未落实，不能据此自行扩大授权。

## 验证证据

开发协作规则（2026-09-25）：仅修改`AGENTS.md`、产品决定和本状态；明确委派条件、文件范围、验收与接管，主Agent负责最终整合及提交。文档差异与引用检查通过，应用代码未变，复用下列验证，不重跑业务测试。无迁移、备份、服务重启或模型配置修改；多模型实际成本收益尚未测量。

回答版本（2026-09-25）：重新生成复用原问题并追加回答版本；操作栏最前方`< n/n >`切换，旧正文、思考、来源与后续路径保留。重生成旧回答不使用旧版后续内容，切回旧版恢复原路径；选择版本后续问仅携带该路径。复制/重新生成/分支改为纯图标及悬停提示，无赞踩/朗读。Provider不可用仍可查看和复制旧版。

- 存储复用既有运行快照JSON，新增带版本号的`reply`关系元数据。普通聊天的新回答记录引用原问题，重生成运行不占用原问题的一对一首轮运行外键；题目讨论各版独立保存，旧的原地覆盖重试接口被拒绝。历史线性记录按原顺序读取，不改写或猜测合并之前重复发送的问题。选中路径只在浏览器保存ID；服务端保存全部版本及每次生成的父关系，API按所属会话/讨论核对。
- 后端全量首轮 **399 passed / 2 failed**，两处均为测试仍调用旧覆盖式重试或错误清理方法；更新这些测试后版本/讨论/回看 **24 passed**，其余未改代码复用结果。新增检查覆盖原问题只存一次、同问题多回答、旧后续隔离、选择路径续问、请求幂等/冲突、跨对话拒绝、取消保留及所有版本彻底删除。
- 浏览器第一轮 **18 passed**，覆盖版本切换、刷新恢复、草稿和历史保留、思考与来源、目标隔离及移动端。补充Provider不可用时历史浏览检查后 **14 passed**；最后修正切换旧版时自动滚动遮住版本按钮的问题，最终构建两条相关流程 **2 passed**，并检查按钮仍在视野内。未改动的其余结果复用。
- 复用已有数据结构，无迁移或新增备份。最终构建 `/tmp/nautilus-reply-versions-release-dist` 已安装，原trial已重启；API健康及WSL网页均200，JS/CSS实际响应字节与测试构建一致，8018/5188均监听0.0.0.0。
- 变更：后端`conversations.py`、`ai_runtime.py`、`question_discussion.py`、schemas与AI/learning路由；`test_reply_versions.py`、讨论流及回看测试；前端`AiLearningRoom`、`QuestionDiscussion`、`LearningRoomLayout`、新增`useReplyHistory`、API类型和学习室样式；AI/验证回看浏览器测试；本状态、产品决定、PRD V2。
- 参考[Open WebUI回答组件](https://github.com/open-webui/open-webui/blob/main/src/lib/components/chat/Messages/ResponseMessage.svelte)的同问题多版本/左右切换模式，独立实现适合现有数据结构的逻辑。DeepSeek网页自动读取403，未宣称直接检查其登录后页面。

## 已知限制与待办

- 首页“已完成”当前表示本次委托/任务完成，不是目标完成；只读核对与代码一致。目标正式收尾的独立流程尚未实现，当前呈现容易混淆，留待后续功能设计明确；本轮不改变既定完成语义或UI。

- 分步教学当前靠每轮提示约束，不是完整苏格拉底提示阶梯/模式运行时；旧AI正文不会被自动删改或重新生成。真实模型的质量、延迟与长回复仍由作者试用；自动化测试不等于学习有效性。旧记录没有保存的逐题反馈或思考不补造；未提交草稿跨刷新不保证恢复。
- 题目讨论最多一次本地关键词检索、6段、每段1600字符、最近8轮。它不是联网搜索；**FOLLOWUP-SEARCH-001** 已进入后续计划，服务商、预算、出站范围待确定。
- 进程运行中刷新可恢复订阅；服务重启后中断轮次保留部分内容供重试，不自动重新调用模型。
- 正则v1确定性检查只证明当前pattern/sample匹配。只有已审核标准参与状态派生；AI反馈、一次通过和独立完成自报都不能直接证明稳定掌握。
- 历史schema v1事件私文处置、双库协调恢复、手工导出/Provider/文件系统副本清除未完成。受控恢复会拒绝复活已清除内容；保留的旧备份不会因后续purge自动被物理改写。
- `GET /agent/context` 是确定性上下文契约，不是完整外部Agent执行环境。成果图、路径图、完整自适应窗口、后台教练、人格和插件平台仍属后续范围。
- 运行网络信任边界、全副本删除承诺、执行完成是否脱离AI验证、扩展试点人群/主题、后续大型能力是否长期冻结，仍需产品决定。作者本人试用已确认，不重新询问本次参与者。
- `frontend-skill` 已在本机Codex配置停用，文件保留；重新加载需重启Codex，Nautilus重启不替代它。

## 下一项

等待作者新窗口的试用反馈或具体需求；检查同一问题重新生成、`< n/n >`切换与切回旧版恢复后续对话，继续积累Task 13B真实使用基线。下一次有适合拆分的任务时按AGENTS执行并观察成本、时间和返工；不为演示委派另开任务。独立分支入口、朗读、完整学习模式运行时和整体前端重构保持后续范围。

## 本次维护与更新记录

- 2026-09-24：按用户授权清理3份旧备份（9个文件，2,559,596字节）；保留最新恢复点，未删除运行数据库、未迁移或重启。
- 2026-09-24：压缩本状态和决策中的重复历史，删除17份被替代的进度/交接报告；精简实施计划，纠正README/架构/PRD/首片审查里的过期描述，修复引用；AGENTS明确原位维护唯一当前状态。需求、未决问题、标准包和有独立设计内容的规格保留。历史全文可从清理前提交 `4e8cdfc` 获取。
- 此后仅更新受影响的当前段落和一条简短历史，完成相关检查后及时本地commit，不push。

- 2026-09-24：完成返回卡、依据过滤、导航及新旧计划数据路径审查，只更新本状态和产品决定；无代码/数据库/服务变更，不跑业务回归，文档差异检查通过。

- 2026-09-24：落实已确认的三入口和首页短命名，撤下旧规划入口；新计划查看/原计划追加/记录跳转与任务切换闭环，呈现组件独立。无迁移或新增备份；测试和启用结果见上文。

- 2026-09-24：按用户要求重新启动原trial（旧进程已退出）；API健康及WSL网页入口均200，8018/5188均监听0.0.0.0。沿用现有构建和数据库，无应用代码改动、迁移或新增备份；复用既有测试。本轮仅更新状态及产品决定，下一项为作者功能试用，前端重构延后单独处理。

- 2026-09-24：核对任务完成与目标完成的区别；修复学习安排低输出额度、缺少JSON约束和混合错误提示，55项后端及2条浏览器定向验证通过，已重启启用。目标收尾仍未实现；未改UI、数据库结构或新增备份。

- 2026-09-25：落实分步教学提示和默认折叠交互，55项后端及15条浏览器验证通过；更新PRD/决定，已安装构建并重启原trial，无迁移或新增备份。

- 2026-09-25：复现并修复新目标错误复用旧学习会话，添加前端归属保护与共用回复操作；后端定向及最终19条浏览器验证通过，已重启启用，无迁移或新增备份。

- 2026-09-25：按用户纠正实现同问题回答版本与前置上下文路径，撤销重复发送式重试；纯图标/悬停说明，无赞踩/朗读。验证和运行状态见当前摘要。

- 2026-09-25：按用户确认将Astra主导/Sol有限委派固化到AGENTS.md，同步决定与状态；仅文档检查，运行服务与产品模型配置不变。
