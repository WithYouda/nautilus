# 学海无涯（Nautilus）首个纵切片与后续开发实施计划


## 2026-09-24 FOLLOWUP-VERIFY-REVIEW-001 交互补修

本轮按用户反馈补齐题目讨论的即时右侧消息、Provider 实际流式回复、刷新重连、取消与幂等重试；查阅记录改为逐条渐变预览/独立展开。复用学习室聊天面板，不修改验证/完成语义，不增加迁移或开放网络工具。工程证据与运行状态见 `docs/progress/nautilus-discussion-streaming-2026-09-24.md` 和开发状态顶部；真实 DeepSeek 复试仍为下一项，FOLLOWUP-SEARCH-001 保持后续。

## 最新执行入口（2026-09-23）

作者本人已获授权使用独立试用环境，正在根据真实反馈修复连续旅程；09-19 的启动 NOT READY 是历史状态。作者已报告完成本次试用并提出回看/续学缺口。验证回看与按题继续学习纵切片已经实现；同日已获明确授权并完成试用学习库 027 → 028 升级与启用。唯一下一项是作者复试历史验证回看、单题续问及刷新恢复，并验证新 DeepSeek 逐题反馈。完整状态以开发状态文档顶部为准。真实结构化基线仍未核验。

### FOLLOWUP-VERIFY-REVIEW-001：验证回看与按题继续学习

状态：2026-09-23 用户已授权按建议继续，工程实现及隔离验收完成；真实试用库已按后续明确授权升级到 028 并启用，本轮新旅程待作者真实复试。本条修补现有学习旅程，不启动 Task 15/16/17B/19/20。

1. 本人验证详情与学习记录入口：题目 → 当次作答 → 对应反馈/AI 参考解法；历史按提交/评估版本关联，刷新和重新进入可回看。摘要列表不返回私人正文；服务端逐次校验 owner 与 purge。旧记录只有整次反馈时明确整体展示，不调用模型伪造历史逐题反馈。完成委托可从正常入口找到，不要求进入事件/证据工具或旧计划列表。
2. 从题目进入学习：复用现有 Provider 和同委托关系，创建与题目、提交及评估版本关联的新讨论；私文保存在学习库，以便和验证内容同事务清除。主库教学对话作为只读检索源；继续反馈中的问题，保留原始验证结果。对话按需实际检索同委托允许使用的历史并引用出处；不是全量聊天复制，也不是仅提示模型“你可以搜索”。优先现有 SQLite/关系，不引入通用工具平台、向量库或后台 Agent。
3. 贯通安全与验收：本人可回看、未授权读取拒绝；跨委托检索拒绝、删除后不能检索/回放恢复；新增对话上下文/运行快照/摘录副本纳入删除设计。覆盖提交与历史逐题对应、旧结果兼容、完成后继续问但不改原结果、历史重新进入、长页滚动和窄屏。

布局：右侧 AI 伙伴默认折叠、保留按需展开已于同日由用户确认并实施；彻底删除已按后续授权下沉到更多/隐私管理，确认时显示关联讨论影响数量。不能以本计划为删除数据或永久取消规划能力的授权。AI 可见参考解法与隐藏评分/内部推理分开；阅览参考解法后的补答不能记录为新的独立表现。

更新历史：2026-09-23 新增本条并覆盖之前“复试验证方式切换”的下一项；调查证据与限制见 `docs/progress/nautilus-verification-review-feedback-2026-09-23.md`。FOLLOWUP-SEARCH-001 保持独立后续任务，供应商、费用和出站范围仍待确定。

实现说明：新增 028 的三个轻量表，不引入通用 Agent、搜索基础设施或独立题库平台。换方向入口在实际选择后暂停当前会话，保存 choose_other/switched 行为，不冒记“今天先停”或推荐完成。详见 `docs/progress/nautilus-verification-review-implementation-2026-09-23.md`。

更新历史：2026-09-23 用户确认备份/升级/重启，本轮范围已执行；转入作者真实旅程验收，不重复迁移或请求同一授权。

### FOLLOWUP-SEARCH-001：额外联网搜索服务配置与最小接入

**状态：已由用户明确要求纳入后续开发计划；尚未实现，不在本次验证入口修复中顺带开通。** 本条来自 2026-09-22 真实试用对联网能力的需求，不依赖 Task 15/16、完整 Research Agent 或通用插件平台。

- 用户问题：使用 DeepSeek 教学/验证时需要真实检索资料，不能把官网功能或模型声称已搜索当成应用实际能力。
- 最小范围：独立搜索服务的选择、地址/必要凭据配置、启停、连接测试；教学模型可继续使用 DeepSeek。默认不自动搜索，先明确发送给搜索服务的查询内容及使用边界。
- 待确认：具体供应商、费用预算、允许出站的信息范围；“列入计划”不等于授权购买、读取既有凭据或向新增服务发送学习正文。
- 接入约束：搜索结果标注来源链接和实际检索状态/时间；只把实际执行的结果称为检索依据，不自动变为人类复核或学习掌握证据。失败、超时或未配置时明确降级，仍可保存学习与作答；不静默换服务。
- 验收：设置在页面可见且可关闭；Mock 测试覆盖启停/失败/不泄密；使用用户选择并配置的服务完成一次真实检索验收。不得用模型生成 URL 代替搜索成功；不扩展任意网页爬取、后台自主浏览或工具平台。
- 排期：当前验证阻塞修复与复试之后，作为独立待办先确认配置契约和供应商，再实施。后续交接必须保留本条状态，不能仅以“待选服务”省略实际开发任务。

更新历史：2026-09-22 按用户明确要求新增 FOLLOWUP-SEARCH-001；原阶段门保持，未自动启动延期的大型能力。

## 当前阶段与停止门（2026-09-19）

历史“完成”不代替本轮动态验证。本轮无生产数据库、提交、push、部署授权。

- Stage A：A1 Evidence provenance hardening → A2 Multi-version purge hardening → A3 Evidence private-copy purge unification → A4 Replay snapshot/concurrency hardening → A5 Measurement contract correction。
- Stage B / Task 17A.0：最小完成后 / 返回时复核与继续学习。后端 read model 汇总上次位置、当前行动、已有依据、未知/缺口、一个建议和理由。界面提供继续、换一个、今天先停、查看依据。完成后立即展示，离开刷新恢复，移动端主操作可达；不能未经确认修改正式计划。规则优先级为进行中、中断、待复核、失败验证、明确缺口、下一开放委托，不依赖大模型。
- Stage C / Task 13B：真实使用为后续功能 Gate。只采结构化展示、推荐、选择、开始、纠正、实际完成；首个产出、AI 初始化及草案修改有结构化记录，不复制原文。确认试点对象和必要运行授权后采集，自动测试不能代替。
- Task 15 full outcome graph、16 full path graph、17B proactive background coach、19 full persona system、20 general plugin platform：**deferred pending real-usage evidence**。是否正式冻结仍待负责人决定。本轮不实施，不是永久取消。
- Task 18、21 和其他路线图条目不自动开始；13B 未支持对应真实需求时不进入扩张。

Gate 1：规范明确来源可信边界、17A.0 与 13B Gate 后进入代码。
Gate 2：A1～A5 负例、清除、并发及指标契约验证通过后进入返回卡。
Gate 3：完成返回、中断恢复、支持/未知分离、单一建议、可拒绝、刷新和窄屏全部成立才算工程完成。
Gate 4：只由真实使用判断真实价值。

### 本轮工程验收结果

- Gate 1 已完成：五份基线先于业务修改同步。
- Gate 2 已完成本轮新写入边界：provenance 负例、多版本 purge、普通证据私文、并发回放、指标关联均通过；历史 v1 私文迁移留在单独授权范围。
- Gate 3 已完成工程切片：完成后卡、中断恢复、拒绝建议、刷新、原对话、390/1024/1440 与 125% 缩放通过。新增 025～027；最终后端全量 340 passed，最终联合浏览器 20 passed（连续旅程 2、旧事实链 5、学习室 13）。详细命令、失败与修正见实施记录。
- Gate 4 未完成：无真实用户样本、无已确认试点人群、无本轮生产升级/运行授权。Task 13B 启动 NOT READY；采集能力已准备，不能用自动化替代真实基线。
- **唯一下一项：完成 Task 13B 试点启动确认**，明确首轮学习者、观察范围、网络与删除承诺边界，再单独安排必要真实环境授权。不能自动进入任何后续图、教练、人格或插件项目。

## 历史任务记录（后续表格依赖不构成自动执行授权）

首片直接使用新领域模型，不建立旧模型映射、双写或适配层。首片围绕一项可验证成果和一个预先审核、版本化的窄领域标准版本工作；无标准时仍保存会话、文本产出和事实事件，但不生成主张或状态。

## 执行规则

- 除 Task 14 在用户明确授权后操作指定的生产学习库外，每个阶段开始前先在隔离数据库执行迁移和备份/恢复检查；不自动迁移任何数据库。
- 不修改或应用 `001` 至 `010` 迁移；Task 14 不得把 `011` 之后的迁移应用到默认主库 `data/nautilus.sqlite3`，未来 schema 变更继续从当前最新编号之后顺序编号。
- 每个领域命令必须先有失败测试，再实现事务、事件和投影。
- 事件账本与当前态投影同事务提交；回放在 BEGIN IMMEDIATE 后读取截止点、校验事件并重建，验证领域投影后提交，失败整体回滚。
- 不把分析失败、无标准或待复核状态写成成功事实。
- 不在日志、测试夹具或进度文档中写入令牌、Cookie、API Key 或用户原文。
- 每完成一组任务，更新 `docs/progress/nautilus-development-status.md`，记录真实状态和下一项。

## 任务总览

| 阶段 | 任务 | 依赖 | 验收门 |
| --- | --- | --- | --- |
| 0 | 规格基线、隔离环境和备份恢复演练 | 无 | 前置 |
| 1 | 新领域 schema 与对象仓储 | 0 | 事实门 |
| 2 | Core 命令、事件账本和事务投影 | 1 | 事实门 |
| 3 | 会话、文本产出和事实回放（后端基础） | 2 | 事实门 |
| 3.1 | 事实基础修复与回放加固 | 3 | 事实门 |
| 4 | 事实门用户流程、权限和 Playwright | 3.1 | 事实门 |
| 4.1 | 事实门正式验收收尾 | 4 | 事实门 |
| 5 | 候选主张、分析失败和幂等重试 | 4.1 | 证据门 |
| 6 | 状态派生、复核、质疑、撤回和重算 | 5 | 证据门 |
| 6.1 | 状态派生与复核收尾 | 6 | 证据门 |
| 7 | 普通删除、彻底删除、证据回放和权限 UX | 6.1 | 证据门 |
| 8 | 指标、全量验证和交接 | 7 | 两门 |
| 9 | 真实标准包与 Provider 正式化 | 8 | 首片产品验收 |
| 10 | Agent 权限批准与最小上下文 | 9 | 首片产品验收 |
| 12 | 事件版本兼容策略与回放硬化 | 10 | 首片产品验收 |
| 11 | 复核卡权限可见范围与证据门验收 | 12 | 首片产品验收 |
| 13A | 指标采集、口径与隐私硬化 | 11 | 首片产品验收 |
| 14 | 生产学习库升级与真实环境启用 | 13A | 生产准备 |
| 15A | 目标初始化与新领域工作轴最小闭环 | 14 | 后续工作轴 |
| 15B | 验证可靠性、学习室恢复与启动迁移保护 | 15A | CAP-VERIFY-001 至 005、INV-008 |
| 15C | 验证产出接入既有事实、证据与删除链 | 15B | INV-001、004、005、007 |
| 17A.0 | 最小完成后/返回时复核与指标修正 | 15C | 连续用户旅程 |
| 13B | 真实使用基线采集 | 17A.0，生产操作另行授权 | 首片产品验收 |
| 15 | 综合成果与成果图 | 17A.0 | CAP-FUT-001 |
| 16 | 学习路径图 | 15 | CAP-FUT-001 |
| 17A | 回归教练视图与自适应承诺窗口 | 16 | CAP-FUT-001 |
| 17B | 主动后台教练 | 17A | CAP-FUT-001 |
| 18 | 模型策略层级与运行配置冻结 | 17B | 完整 PRD V2 |
| 19 | 人格 Profile 与学习模式最小闭环 | 18 | 完整 PRD V2 |
| 20 | 知识包与能力插件治理最小边界 | 19 | 完整 PRD V2 |
| 21 | 统一工作台与 PRD V2 整体验收 | 20 | 完整 PRD V2 |

### 2026-09-16 增量执行条目

- Task 15B：023+ 追加迁移；独立保存、评估、确认 API，幂等提交和评估历史，迟到结果隔离，最终确认事务、多委托完成事件与回放；验证恢复和真实会话/对话归属；拒绝不合法结果与占位来源；普通应用启动只检查版本。验收覆盖保存先于 Provider、故障重试、权限、并发、确认不重评、回滚、回放和旧库升级，不用低价值纯 UI Playwright 代替这些检查。
- Task 15C：复用已有 raw artifact、fact、claim、derived state 与彻底删除机制，不把 AI 自拟评分当成已审核标准；先验证既有 Python 正则标准，无标准只保存反馈。迁移旧验证内容时保留来源，不虚构过去没有保存的作答。
- Task 17A.0：提供当前位置、已有依据、未知、一个推荐行动及理由和继续入口；修正完成/中断/主动切换/未知的测量口径，产品基线不预设 100% 通过线。该最小界面无需 Task 15/16 图形能力，完整自适应窗口仍在 Task 17A。
- 每段完成后更新实现事实，后续条目不因排期采纳而记为完成。真实库检查/备份/迁移及启动必须另行授权。

执行状态（2026-09-16）：Task 15B 与 Task 15C 已完成本段代码和聚焦验证，024 仅应用于隔离库；Task 17A.0 尚未开始。15C 的事实关联、标准准入、参考材料隔离、删除/回放/备份边界、文件和测试记录见 `docs/progress/nautilus-verification-evidence-implementation-2026-09-16.md`。生产启用、人工旅程与真实基线仍未验收。

## Task 0：规格基线、隔离环境和恢复演练

**覆盖**：`INV-008`、`CAP-MEASURE-001`

**依赖**：无。

**文件范围**：

- 新增或更新 `docs/superpowers/specs/2026-09-05-nautilus-first-slice-domain-architecture.md`；
- 新增本计划；
- 更新 `docs/progress/nautilus-development-status.md`；
- 复用 `backend/tests/conftest.py` 的临时数据库机制，必要时新增隔离数据库工具。

**工作内容**：

- 固定测试数据库目录、迁移版本检查、备份文件命名和恢复校验命令。
- 为事件账本、原始产出和投影定义不包含用户原文的测试夹具。
- 演练“备份 -> 新临时库恢复 -> 完整性校验 -> 删除临时库”，确认默认数据库不被打开或修改。

**完成标准**：

- 隔离数据库可以重复创建、迁移、备份和恢复；
- 迁移版本和 `integrity_check` 可验证；
- 恢复演练结果写入开发状态，不写入私密内容；
- 明确默认数据库仍未迁移，等待后续单独授权。

## Task 1：新领域 schema 与对象仓储

**覆盖**：`INV-001`、`INV-005`、`INV-009`、`CAP-FACT-001`

**依赖**：Task 0。

**文件范围**：

- 新增 `backend/app/migrations/011_nautilus_learning_domain.sql`（名称可在实现前微调，但必须以 `011_` 开始）；
- 修改 `backend/app/db.py`、`backend/app/schemas.py` 和新增领域模块，例如 `backend/app/learning_domain.py`；
- 新增 `backend/tests/test_learning_domain_schema.py`。

**工作内容**：

- 建立成果、标准包/版本、学习行动、委托、会话、原始产出、事实事件、幂等记录和分析运行的最小表结构。
- 为原始产出保存内容版本、内容哈希、隐私范围、软删除/彻底删除状态和关联会话。
- 为标准版本保存审核状态、来源、适用范围和版本号；禁止未审核版本进入正式绑定。
- 为事件表建立 `event_id`、聚合版本、事件版本、命令/幂等键、用户范围和投影位置的唯一/检查约束。
- 不在本任务加入证据状态的宽泛缓存或旧计划层级映射。

**完成标准**：

- 隔离库成功应用 `011`，旧迁移文件未改；
- 关键唯一键、外键、CHECK 约束和用户范围约束由测试验证；
- 无标准、非审核标准和越权对象不能通过仓储层校验；
- 数据库迁移失败不会留下半完成 schema。

## Task 2：Core 命令、事件账本和事务投影

**覆盖**：`INV-002`、`INV-003`、`INV-010`、`CAP-FACT-001`、`CAP-FACT-002`

**依赖**：Task 1。

**文件范围**：

- 新增或修改 `backend/app/core/` 下的命令、聚合和投影模块；
- 新增 `backend/tests/test_learning_domain_commands.py`。

**工作内容**：

- 实现命令校验、用户范围校验、幂等键登记、事件追加和关系型当前态更新。API 接入归 Task 4。
- 事件追加、投影更新和必要审计元数据必须在同一事务内完成。
- 重复命令返回首次结果或明确冲突，不重复追加事实。
- 引入投影位置和聚合版本检查，拒绝缺口、乱序和重复事件。
- 让 Runtime/Agent 只能通过 Core 命令提交，不允许直接写领域表。

**完成标准**：

- 命令成功、事务回滚、重复重试、并发版本冲突均有测试；
- 跨用户读取和写入被拒绝；
- 事件和当前态无法出现一方提交、一方失败的部分结果；
- 事实门所需事件可按聚合顺序查询。

## Task 3：会话、文本产出和事实回放（后端基础）

**覆盖**：`INV-001`、`INV-002`、`INV-004`、`INV-005`、`CAP-FACT-001`、`CAP-FACT-002`

**依赖**：Task 2。

**文件范围**：

- 修改 `backend/app/core/`；
- 新增 `backend/tests/test_learning_facts.py`。

**工作内容**：

- 支持开始、结束和中断学习会话；保存文本原始产出和对应事实事件。
- 将产出内容和发生上下文分开持久化；Core 读取接口可以取回保存结果。
- 保存链路不依赖 AI 可用性；无标准时记录明确降级原因，不生成主张或学习状态。
- 产出更正通过追加版本和事件表达，旧版本不可覆盖。
- 提供事件读取和首版回放校验；完整回放加固归 Task 3.1。

**完成标准**：

- 后端会话、产出、更正、幂等和事实事件在隔离数据库通过集成测试；
- 原始产出不可覆盖，更正入口只创建新版本/追加事件；
- API/UI/浏览器流程不在本任务验收，归 Task 4。

## Task 3.1：事实基础修复与回放加固

**覆盖**：`INV-002`、`INV-003`、`INV-004`、`INV-005`、`INV-010`、`CAP-FACT-001`、`CAP-FACT-002`

**依赖**：Task 3。

**文件范围**：

- 修改 `backend/app/core/learning.py`、`backend/app/core/events.py`；
- 新增 `backend/tests/test_learning_fact_hardening.py`；
- 更新本计划、实施记录和开发状态文档。

**工作内容**：

- 按 `(owner_id, aggregate_type, aggregate_id)` 校验聚合版本 1..N 和 `previous_hash` 链，不得把全局事件位置当作单用户序列。
- 回放完整重建行动、成果、委托、契约、会话、产出当前态、分析降级记录、流末端和投影位置；不可变原始产出作为历史依据保留并校验版本、事件和内容哈希。
- 回放在同一事务中校验并原子切换；失败保留旧投影并写拒绝原因。
- 更正版本号在事务内根据当前最高版本推导，支持连续多次更正。
- 有合格标准时不创建 `blocked_no_criterion`，分析运行留到证据门；无标准时才创建该降级记录。
- `execute`、`events`、`artifact` 和 `replay` 使用统一的用户身份/归属校验，Agent 与 actor/owner 不一致请求被拒绝。

**完成标准**：

- 多用户事件交叉、完整投影重建、回放后续写、连续更正、标准状态、Agent 拒绝、身份不一致拒绝、断链和不受支持版本均有回归测试；
- 定向后端测试和全量后端测试通过；
- 事实门仍未最终通过，Task 4 负责真实 API/UI/Playwright 验收。

## Task 4：事实门用户流程、权限和 Playwright

**覆盖**：`INV-003`、`INV-008`、`CAP-FACT-001`、`CAP-FACT-002`

**依赖**：Task 3.1。

**文件范围**：

- 修改 `backend/app/routers/`、`backend/app/schemas.py` 和事实门涉及的 `frontend/src/*.tsx`、样式文件；
- 新增 `frontend/e2e/nautilus-first-slice-facts.spec.ts`；
- 扩展 `backend/tests/test_permissions.py`（或按现有测试布局命名）。

**工作内容**：

- 验证无标准时仍能保存会话、文本产出和事实事件，界面明确显示不可分析原因。
- 验证全局 Agent 最小范围、权限申请、拒绝后继续保存和跨身份隔离。
- 接入后端事实命令与读取 API，覆盖桌面和移动端的创建委托、开始会话、保存文本、结束会话、重新打开和回放状态。
- 检查失败状态、长文本、重复点击和刷新恢复不会造成重复产出。

**完成标准**：

- 事实门所有验收条件通过后，才允许开始证据门；
- Playwright 使用隔离数据目录，不访问默认 `data/`；
- 无跨身份越权、未授权写入或保存链路阻塞；
- 结果记录到开发状态并标明测试数据范围。
- 2026-09-07 实施状态：API、独立学习库、前端事实工作台、无标准降级与分析运行状态、回放、刷新恢复、跨身份隔离、Agent 拒绝和 Playwright 已完成并通过自动化验证；事实门仍不得宣布通过，正式验收收尾归 Task 4.1。

## Task 4.1：事实门正式验收收尾

**覆盖**：`INV-003`、`INV-005`、`INV-008`、`INV-010`、`CAP-FACT-001`、`CAP-FACT-002`

**依赖**：Task 4。

**文件范围**：

- 修改统一 Agent Runtime、权限申请/拒绝链路和相关测试；
- 引入一个真实预审核、版本化的窄领域标准包或明确记录首片暂以无标准路径验收的边界；
- 补齐事实门验收清单、指标记录和必要的投影切换/范围测试；
- 更新实施记录和开发状态。

**工作内容**：

- 将 Agent 权限申请从当前界面演示升级为真实 Runtime 请求、拒绝、继续保存和审计链路。
- 复核 PRD 11.4.1 的 9 条验收条件，逐项记录证据、测试命令和未测原因。
- 明确同一用户内部范围在新领域模型中的首片映射，避免用跨身份测试替代 Agent 授权测试。
- 复核回放的“隔离投影 + 校验后切换”语义与当前事务内重建实现的差异，必要时改为临时投影或记录已批准的技术替代。
- 建立真实窄领域标准包或在验收记录中明确本门仅验证无标准降级，不把合成测试标准冒充真实标准。

**完成标准**：

- PRD 11.4.1 逐项有关闭结论和可复现证据；
- Agent 权限申请/拒绝不再依赖纯界面演示；
- 事实门正式通过前，Task 5 不得开始。

**2026-09-07 实施状态**：已新增持久化 `learning_agent_permission_request`、统一 `AgentRuntime`、权限申请/拒绝 API、前端真实申请与拒绝链路、审计和跨身份隔离测试；PRD 11.4.1 已形成逐项验收记录，事实门按“无标准路径 + 当前新领域行动范围”关闭；后端 195 项、Playwright 33 项通过。真实窄领域标准包仍未建立，批准后的 Agent 实际读取授权尚未实现，证据门开始前必须先补齐标准包。

## Task 5：候选主张、分析失败和幂等重试

**覆盖**：`INV-004`、`INV-005`、`INV-006`、`CAP-EVID-001`、`CAP-EVID-002`

**依赖**：Task 4.1；事实门必须通过，且必须先确定首个窄领域和真实预审核标准包。

**文件范围**：

- 新增或修改 `backend/app/evidence.py`、`backend/app/ai_runtime.py`、相关路由和 schema；
- 新增 `backend/tests/test_evidence_claims.py`、`backend/tests/test_analysis_failures.py`；
- 修改 `frontend/src/` 的分析状态、失败恢复和候选主张界面。

**工作内容**：

- 只允许有效标准和已保存产出触发分析。
- 候选主张同时引用产出 ID/版本和事实事件 ID，记录来源、验证方式和证据范围。
- 区分不可用、超时、取消、输出不合格、权限拒绝和内部错误。
- 允许重试、请求人工复核和安排补充验证；幂等重试不得产生重复主张。
- 无标准直接生成 `blocked_no_criterion`，不调用状态分析。

**完成标准**：

- 六类 AI 故障场景均能读取保存结果并恢复；
- 失败运行有审计记录，但没有空主张或伪造状态；
- 相同请求重复提交只得到一个业务结果；
- 候选主张引用链可由 API 和界面展开查看。
- 2026-09-07 实施状态：已审核标准包 `python-regex-basics-v1` 自动种入；Provider 语义分析、确定性检查、候选主张、六类失败恢复、人工复核请求、补充验证安排和幂等重试已实现。后端 209 项、Playwright 34 项通过。状态派生和复核执行归 Task 6，证据门尚未正式验收。

## Task 6：状态派生、复核、质疑、撤回和重算

**覆盖**：`INV-006`、`CAP-EVID-001`、`CAP-MEASURE-001`

**依赖**：Task 5。

**文件范围**：

- 新增或修改 `backend/app/state_derivation.py`、`backend/app/review.py`；
- 修改相关路由、schema 和前端复核卡；
- 新增 `backend/tests/test_state_derivation.py`、`backend/tests/test_review_workflow.py`。

**工作内容**：

- 按标准版本和证据配方计算成果维度状态及原因标签。
- 将候选、采纳、质疑、撤回、替代分开处理；采纳不直接设置稳定状态。
- 支持查看依据、采纳符合条件的主张、质疑、暂不处理、请求人工复核和安排验证。
- 质疑或撤回立即移出当前计算，保留历史派生结果和审计记录。
- 标准变更、事实更正或证据适用性变化触发可追溯重算。

**完成标准**：

- 矛盾证据不被简单正负抵消；
- 状态结果能展示参与证据、排除原因、标准版本和计算版本；
- 质疑/撤回后的当前状态和回访队列正确重算；
- 用户认可不能绕过 Core 标准规则直接升级状态。
- 2026-09-07 实施状态：状态派生、采纳/质疑/撤回/替代/暂不处理、复核审计、历史派生记录、排除原因、矛盾证据处理、产出更正后的旧主张替代和状态重算已实现；后端 217 项、Playwright 35 项通过。批量复核、替代关系、回访队列和证据门正式验收归 Task 6.1。

## Task 6.1：状态派生与复核收尾

**覆盖**：`INV-006`、`CAP-EVID-001`、`CAP-MEASURE-001`

**依赖**：Task 6。

**文件范围**：

- 修改 `backend/app/review.py`、`backend/app/state_derivation.py`、相关路由和 schema；
- 修改前端复核卡和批量操作入口；
- 新增或扩展 `backend/tests/test_review_workflow.py`、`backend/tests/test_state_derivation.py`；
- 新增证据门验收记录。

**工作内容**：

- 支持同质低风险候选主张的批量复核，并保持每条主张独立审计。
- 为替代主张建立显式替换关系，不把 `superseded` 仅当作状态标签。
- 建立回访队列，质疑、证据不足和补充验证到期后进入待回访。
- 明确标准版本不可变前提下的重算触发；若未来允许标准包升级，必须新增版本并触发全量重算。
- 逐项复核 PRD 11.4.2 中属于本阶段的状态派生与复核条款。

**完成标准**：

- 批量操作不会绕过单条主张的资格校验；
- 替代、质疑、撤回和补充验证的回访路径可追溯；
- PRD 11.4.2 相关条款有可复现证据；
- Task 6.1 阶段验收通过后，Task 7 才能开始。

**2026-09-07 实施状态**：已新增批量复核、显式替代关系、回访队列、标准新增版本后的全量重算和阶段验收记录；后端 220 项、Playwright 35 项通过。证据门整体仍未通过，删除/回放/权限 UX 归 Task 7。

## Task 7：普通删除、彻底删除、证据回放和权限 UX

**覆盖**：`INV-003`、`INV-006`、`INV-007`、`INV-008`、`CAP-EVID-001`、`CAP-EVID-002`

**依赖**：Task 6.1。

**文件范围**：

- 修改领域命令、删除/索引/缓存处理模块和相关路由；
- 修改 `frontend/src/` 删除确认、影响预览、恢复和权限申请组件；
- 新增 `backend/tests/test_artifact_lifecycle.py`、`backend/tests/test_privacy_deletion.py`；
- 新增 `frontend/e2e/nautilus-first-slice-evidence.spec.ts`。

**工作内容**：

- 普通删除作为正常可用的软删除并支持恢复；不影响历史审计内容的权限边界。
- 撤回只改变证据适用性，不删除产出。
- 事实纠正通过追加版本表达，旧版本不被覆盖。
- 彻底删除显示受影响主张、状态和回访安排，要求明确确认；清除/不可逆脱敏内容并使依赖主张失效。
- 删除后同步清理缓存、搜索索引和临时文件；普通备份不得继续提供已彻底删除内容。
- 验证复核卡、全局 Agent 和跨身份请求不会暴露已删除内容。

**完成标准**：

- 普通删除、撤回、事实更正和彻底删除四条路径的行为与恢复边界互不混淆；
- 彻底删除后状态、回访队列和证据回放结果正确；
- 不能从缓存、索引或普通恢复流程取回已删除原文；
- 审计记录只保留最小元数据，不含原始私人内容。

**2026-09-08 实施状态**：已实现普通删除/恢复、证据撤回、彻底删除、产出更正替代、证据事件账本与回放、跨身份隔离和 Agent 权限不暴露已删除内容；后端 227 项、Playwright 36 项通过。缓存、搜索索引和产品级备份清理当前无可清理对象，现有恢复工具仅为测试夹具。证据门整体验收仍归 Task 8。
## Task 8：指标、全量验证和交接

**覆盖**：`CAP-MEASURE-001`、全部适用 `INV-*`、`CAP-FACT-*`、`CAP-EVID-*`

**依赖**：Task 7。

**文件范围**：

- 修改事件/审计记录和测量查询模块；
- 新增或修改 `backend/tests/test_measurements.py`；
- 完整运行 `backend/tests/`、前端构建和 Playwright；
- 更新 `docs/progress/nautilus-development-status.md`。

**工作内容**：

- 按固定口径记录正确恢复委托比例、复核到下一行动完成率和证据可追溯率。
- 记录无依据主张率、指定 AI 故障场景产出保存成功率、事件重建一致性、权限违规和隐私泄露等硬性护栏。
- 零分母标记为无样本；未知结果、权限拒绝和待验证单列，不能美化完成率。
- 运行后端单元/集成测试、前端生产构建、事实门和证据门 Playwright、隔离迁移/恢复/回放验证。
- 更新状态文档，列出实际变更文件、迁移是否应用、测试结果、已知问题和下一项任务。

**完成标准**：

- 所有适用需求编号均有“已实现/已验证/未实现”的明确状态；
- 硬性护栏全部通过，否则不得宣称首片成功；
- 指标未产生样本时明确标记未测，不扩张功能凑指标；
- `git diff --check` 通过；
- 默认数据库仍未迁移，任何迁移时机和恢复方案另行取得用户明确授权。
- 2026-09-08 实施状态：已新增测量服务、指标 API、前端指标卡和测量测试；后端 230 项、Playwright 36 项通过。计划内 Task 0-8 完成，最终交接见 `docs/progress/nautilus-first-slice-final-acceptance-2026-09-08.md`。

## 后续开发计划

### Task 9：真实标准包与 Provider 正式化

**覆盖**：`INV-005`、`CAP-FACT-001`、`CAP-EVID-001`

**依赖**：Task 8。

**文件范围**：

- 修改 `backend/app/standards/`、`backend/app/evidence.py`、`backend/app/ai_runtime.py`；
- 新增真实标准包定义、审核记录和对应测试；
- 修改 Provider 配置与运行时选择逻辑；
- 更新实施记录和开发状态。

**工作内容**：

- 选定一个真实窄领域，不再使用合成标准包作为生产路径。
- 建立真实、可追溯、版本化的标准包，包含来源、版本、能力维度、帮助条件和最低证据组合。
- 明确 Provider 选择、模型策略、超时和重试边界。
- 将真实标准包接入现有语义分析与确定性检查链路。
- 保留合成标准包仅用于自动化测试，禁止其进入生产状态派生。

**完成标准**：

- 真实标准包通过审核并版本化；
- Provider 配置可追溯，模型策略不扩大数据或工具权限；
- 生产路径不再依赖合成标准；
- 相关测试和验收记录完整。
- 2026-09-09 实施状态：已完成。真实 Python 正则标准包沿用已审核 v1；新增证据分析专用 Provider 选择、失败关闭、恢复默认、非敏感运行快照、超时/人工重试边界和前端设置卡。后端 238 项、前端构建、Playwright 36 项通过。新增迁移 `017` 与 `018`，均只在隔离学习库验证。

### Task 10：Agent 权限批准与最小上下文

**覆盖**：`INV-003`、`INV-010`、`CAP-EVID-002`

**依赖**：Task 9。

**文件范围**：

- 修改 `backend/app/agent_runtime.py`、`backend/app/routers/learning.py`、`backend/app/schemas.py`；
- 修改前端权限申请、批准、拒绝、过期和撤销 UI；
- 新增或扩展 Agent 权限与上下文测试。

**工作内容**：

- 实现批准后的最小读取授权，不扩大写入、委托或事实确认权。
- 实现授权过期、撤销和再次申请。
- 全局 Agent 默认只读取跨计划最小摘要、状态和证据引用。
- 确保摘要和引用不能泄露未授权原文。
- 权限申请、批准、拒绝、过期和撤销全部写入审计。

**完成标准**：

- PRD 11.4.2 第 11-13 条全部通过；
- 未授权原文、摘要和引用不可泄露；
- 拒绝后用户仍可继续学习、保存会话和产出。
- 2026-09-10 实施状态：已完成。新增 `019_agent_permission_grants.sql`、批准/撤销/上下文 API、最小全局与行动上下文、分析限制说明、前端批准/拒绝/撤销/再次申请和上下文预览。后端 248 项、前端构建、Playwright 36 项通过。本任务只关闭 PRD 11.4.2 第 11-13 条，不代表证据门整体关闭。

### Task 12：事件版本兼容策略与回放硬化

**覆盖**：`INV-002`、`CAP-FACT-002`、`CAP-EVID-001`

**依赖**：Task 10。

**文件范围**：

- 修改 `backend/app/core/events.py`、`backend/app/evidence_events.py`；
- 新增事件版本支持范围与明确拒绝策略；
- 扩展回放、迁移和版本兼容测试。

**工作内容**：

- 定义当前生产事件版本的支持范围和兼容读取规则。
- 未知版本、未来版本或无法按原语义解释的事件必须结构化拒绝重建，并要求明确迁移。
- 拒绝重建时不得覆盖现有投影，必须保留失败审计记录。
- 使用测试专用的不支持版本号验证拒绝路径；不为关闭验收凭空发明生产事件版本 2。
- 只有确实引入事件版本 2 时，才另行制定显式升级器、迁移范围和回滚方案。

**完成标准**：

- 事件版本变化有确定性处理；
- 无法升级或无法解释时明确拒绝，不按新含义静默解释旧事件；
- 回放一致性和失败保护均有测试；
- PRD 11.4.2 第 9 条关闭。
- 2026-09-10 实施状态：已完成。新增 `020_evidence_event_schema_version.sql`，将证据事件语义版本与聚合内顺序分离；事实与证据事件均显式声明当前仅支持版本 1；证据回放补齐哈希、事件类型、版本和链顺序校验。未引入生产事件版本 2。后端 254 项、前端构建、Playwright 36 项通过。

### Task 11：复核卡权限可见范围与证据门验收

**覆盖**：`INV-003`、`INV-006`、`CAP-EVID-001`、`CAP-EVID-002`

**依赖**：Task 12。

**文件范围**：

- 修改 `frontend/src/FactWorkspace.tsx` 或后续复核卡组件；
- 修改 `backend/app/review.py`、`backend/app/state_derivation.py`；
- 新增证据门最终验收记录；
- 扩展相关后端与 Playwright 测试。

**工作内容**：

- 复核卡明确展示依据的可见范围和权限限制。
- 未经授权的原文或摘录不进入卡片或 Agent 上下文。
- 用户可以暂不处理或安排验证，不能因缺少权限被迫授权。
- 批量处理、逐条依据、暂不处理和人工复核入口符合 PRD 11.4.2。
- 在 Task 12 已关闭事件版本处理边界后，逐条复核证据门 14 条验收条件。

**完成标准**：

- PRD 11.4.2 全部条目有可复现证据；
- 证据门整体产品验收通过；
- 不再存在“工程链路通过但产品验收未关闭”的边界。
- 2026-09-10 实施状态：已完成。复核卡新增依据可见范围、Agent 权限限制和无需授权的替代路径；PRD 11.4.2 14 条逐项验收记录见 `docs/progress/nautilus-evidence-gate-final-acceptance-2026-09-10.md`。证据门整体产品验收通过。

### Task 13A：指标采集、口径与隐私硬化

**覆盖**：`CAP-MEASURE-001`

**依赖**：Task 11。

**文件范围**：

- 修改 `backend/app/measurements.py`；
- 修改前端指标展示；
- 新增指标来源事件、删除语义和报表测试；
- 更新指标验收记录。

**工作内容**：

- 固定观察窗口、分母、分子、排除条件和单列情形。
- 持久化或关联中断后首次恢复委托选择、复核安排与下一行动完成的事实来源。
- 采集中断后正确恢复委托比例、复核到下一行动完成率、证据可追溯率和硬性护栏。
- 指标不得复制原始产出、凭据、Cookie、API Key 或私密摘录；只保存计数、口径版本、时间段、原因码或最小审计元数据。
- 彻底删除后，相关样本必须失效、剔除或单列为“已因彻底删除失效”；历史统计不能成为继续保留私人内容的理由。
- 指标查询、汇总和导出受既有权限边界约束。
- 不为凑指标扩张功能。

**完成标准**：

- 指标口径、来源事件、观察窗口和单列情形在隔离环境可复现；
- 隐私删除后的指标失效或单列路径有测试；
- 真实样本尚未产生时明确标记“无样本”，不伪装为 0% 或 100%。
- 2026-09-10 实施状态：已完成。指标版本升级为 2.0；恢复委托和复核到下一行动改为基于事实记录计算；新增 7 天观察窗口、单列计数、彻底删除失效/单列和隐私输出测试。后端 259 项、前端构建、Playwright 36 项通过。本任务只完成指标工程，不代表真实使用基线已采集。

### Task 13B：真实使用基线采集

**覆盖**：`CAP-MEASURE-001`

**依赖**：Task 15A。

**文件范围**：

- 复用 Task 13A 的指标采集与报表能力；
- 新增真实使用基线记录；
- 更新指标验收记录和开发状态。

**工作内容**：

- 在用户授权启用的真实环境中采集固定统计时段的基线。
- 报告产品价值指标、硬性护栏、诊断指标、无样本原因和窗口未满情形。
- 不通过新增功能、自动触发或虚假样本凑齐数据。
- 不把测试样本冒充真实使用样本。

**完成标准**：

- 产品价值指标有真实样本或明确无样本原因；
- 硬性护栏全部通过；
- 指标报告可追溯、可复现，不美化结果。
- 2026-09-10 实施状态：已开始。真实环境已启用，但生产学习库尚无用户授权身份和真实学习行动，因此当前不能生成真实使用样本，也不能把自动化测试样本冒充真实样本。等待用户在 Web 入口完成真实学习和复核动作后采集基线。

### Task 14：生产学习库升级与真实环境启用

**覆盖**：`INV-008`、`INV-009`

**依赖**：Task 13A。

**文件范围**：

- 新增生产学习库升级与回滚方案；
- 修改 `backend/app/db.py`、`backend/app/learning_storage.py`；
- 新增备份、恢复、迁移和回滚验收测试；
- 更新部署与运行文档。

**工作内容**：

- 制定 `data/learning.sqlite3` 从当前版本升级到最新隔离验证版本的正式方案。
- 在用户明确授权后执行备份、迁移、完整性校验和回滚演练。
- 恢复演练必须验证彻底删除后的内容不会通过普通备份或恢复流程重新出现。
- 不把 `011` 之后的迁移应用到默认主库 `data/nautilus.sqlite3`，不建立旧模型映射、双写或适配层。
- 默认主库的保留、只读或退役边界单独记录，不由本任务隐式决定。
- 明确真实环境运行边界；交付运行说明必须绑定 `0.0.0.0` 并报告 WSL2 实际 IP。
- 未授权时不自动迁移、不启动服务、不修改默认主库。

**完成标准**：

- 用户明确授权迁移；
- 备份、恢复和回滚路径可验证；
- 生产学习库可运行首片与后续功能；
- 默认主库未被 `011+` 迁移改写；
- 未授权时不发生任何生产数据变更。
- 2026-09-10 实施状态：已完成。操作前发现 `data/learning.sqlite3` 不存在，因此初始化新的空学习库并直接应用 `011-020`；未发生既有 011 数据升级。生产库、备份、完整性、外键、恢复演练和彻底删除保护均已验证。用户授权后交付服务已启动并通过健康检查；默认主库按启动机制应用 `005-010`，未应用 `011+`。详见 `docs/progress/nautilus-production-learning-database-2026-09-10.md`。

### Task 15A：目标初始化与新领域工作轴最小闭环

**覆盖**：PRD V2 第 4.2、5.1、8.1、8.2 节和产品决策记录第 14、17 章

**依赖**：Task 14。

**文件范围**：

- 新增从 `021_*.sql` 开始的顺序迁移；
- 修改领域 Core、仓储、API 和前端工作台；
- 新增目标、计划、任务、委托和会话的最小集成与 Playwright 测试。

**工作内容**：

- 实现学习意图表达、可逆目标假设和低成本第一步。
- 在一次用户确认命令中创建目标、计划、任务、可验证成果和学习委托；计划可以直接拥有任务。
- 保留可选模块的数据结构，但本任务不实现模块编辑器；模块化执行树在后续任务继续。
- 建立目标、计划、任务、成果、学习委托和学习会话的显式关系。
- 确认事件、幂等结果和事实回放保留首个工作轴的审计与恢复能力。
- 权限、隐私范围和上下文范围沿用统一 Core/Runtime 边界。
- 不引入旧四层模型兼容、映射、双写或旧入口适配。

**完成标准**：

- 用户可以从自然语言目标开始，选择 AI 整理或手动起步，编辑草案并确认；
- 确认后目标、计划、任务、成果和委托一次创建并可直接进入学习会话；
- 计划直接拥有任务的主路径可用；无合格标准时仍可保存事实并明确降级；
- 确认事件、关系、幂等重试、失败回滚和 owner 隔离回放有自动化测试；
- 已确认的工作轴刷新后仍可读取和继续；未确认草案暂不承诺跨刷新恢复；
- 不因补齐工作轴复制证据或重置历史；模块编辑、焦点管理、对象变更版本和完整探索平台不在本任务完成范围。

**2026-09-13 实施状态**：已完成最小闭环。用户可以从自然语言目标开始，选择 AI 整理或手动起步，编辑并确认草案；确认后一次创建目标、计划、任务、可验证成果和学习委托，并直接进入学习会话。新增 `021_learning_guided_setup.sql` 仅在隔离测试库验证；同时修复同一任务重入时恢复错误 AI 会话的问题。Task 15A 不包含完整模块编辑、探索平台、成果图、路径图或教练视图。

### Task 15：综合成果与成果图

**覆盖**：`CAP-FUT-001`

**依赖**：Task 15A。

**文件范围**：

- 新增综合成果与可验证成果关系模型；
- 修改领域 Core、API 和前端成果图组件；
- 新增成果图、证据覆盖、权限和撤销测试。

**工作内容**：

- 建立综合成果与可验证成果的显式关联。
- 复用已有成果身份、标准版本和证据引用。
- 不复制证据、不重置历史。
- 展示成果图和个人证据覆盖层。
- 保持成果图与计划树、路径图和对话分支的职责分离。
- AI 只能提出关系候选；用户确认后才写入正式关系。
- 综合成果不能仅因子成果部分支持而自动宣布成立。

**完成标准**：

- 用户可以创建综合成果、关联已有可验证成果、查看证据覆盖、调整关系并撤销调整；
- 关系和证据引用可追溯，版本、审计、权限和历史保留可测试；
- 不因补上综合成果改变既有事实；
- 图形只承载长期成果关系和证据覆盖，不演变为通用图数据库或无限可视化平台。

### Task 16：学习路径图

**覆盖**：`CAP-FUT-001`

**依赖**：Task 15。

**文件范围**：

- 新增路径节点、分支和主线模型；
- 修改领域 Core、API 和前端路径图组件；
- 新增路径图与计划树/成果图/对话分支的集成测试。

**工作内容**：

- 建立长期路线节点、可追溯分支、起点变化和可恢复位置。
- 保持路径图只表达路线连续性，不承载普通任务、单次会话或每条消息。
- 用户确认后才改变当前主线。
- AI 可以建议切换或恢复，但不能自动写入。
- 起点变化、暂停分支、恢复位置和变更原因保留历史。

**完成标准**：

- 用户可以调整起点、创建分支、暂停方向、恢复历史位置并查看当时变更原因；
- 路径图、计划树、成果图和对话分支职责清晰；
- 主线变化、分支恢复、版本、审计、权限和撤销边界通过测试；
- 不把细碎消息和普通操作全部暴露为图节点。

### Task 17A：回归教练视图与自适应承诺窗口

**覆盖**：`CAP-FUT-001`、PRD V2 第 1.2、4.1、4.3、8.4 节和产品决策记录第 13 章

**依赖**：Task 16。

**文件范围**：

- 修改学习教练角色契约、状态查询 API 和前端默认回归视图；
- 新增自适应承诺窗口、下一步推荐和理由追溯测试；
- 修改指标和审计记录。

**工作内容**：

- 实现默认回归教练视图：用户回来时能看到当前位置、已有证据支持的成果、未知和缺口、下一步推荐及理由，并能进入下一步行动。
- 提供恢复原委托、选择其他行动或查看依据的入口。
- 推荐必须能追溯到状态、事件、证据和规则，不能只来自模型偏好。
- 未授权原文、摘要和引用不得进入视图或 Agent 上下文。
- 实现最小自适应承诺窗口：初期只承诺少量学习会话，随真实执行、证据和节奏信号逐步扩展；不输出虚假的长期精确排期。
- 推荐或窗口调整不得自动修改正式计划、目标、路径或长期状态。

**完成标准**：

- 用户中断后重新进入时，默认视图满足 PRD 1.2 核心承诺；
- 下一步推荐、理由、证据缺口和权限边界可测试；
- 自适应承诺窗口只基于真实信号，不伪装成精确预测；
- 用户选择、确认和恢复路径可审计。

### Task 17B：主动后台教练

**覆盖**：`CAP-FUT-001`

**依赖**：Task 17A。

**文件范围**：

- 修改统一 Agent Runtime、学习教练角色契约和前端提醒组件；
- 新增事件门控、会话边界和批量复盘测试；
- 修改指标和审计记录。

**工作内容**：

- 实现低成本事件记录、当前 Agent 运行复用、会话边界批量判断和计划级复盘。
- 非阻塞信号进入候选队列，在自然停顿或会话结束呈现。
- 用户明确要求、当前工作无法继续或高影响变化时才即时提示。
- 正式创建、路线调整或长期状态变化仍需用户确认。
- 建立预算、冷却时间和用户开关。
- 不使用多个常驻 Agent 进程，不自动修改正式计划或长期状态。

**完成标准**：

- 主动提醒不频繁打断学习；
- 候选创建、提醒、确认、拒绝、忽略和撤销可审计；
- 成本控制和用户自主边界通过测试。

### Task 18：模型策略层级与运行配置冻结

**覆盖**：PRD V2 第 7.5 节

**依赖**：Task 17B。

**工作内容**：

- 实现全局、计划、任务、对话和单次运行的模型策略覆盖。
- 未设置字段继承上级；本次运行冻结实际配置并可追溯。
- 记录 Provider、模型、配置版本、来源和错误状态。
- 模型能力或更强模型不得扩大数据范围、工具权限或事实写入权。
- Nautilus 只能建议升级，不能未经确认自动切换 Provider、模型或策略。

**完成标准**：

- 策略覆盖、继承、冻结、追溯和不扩权均有测试；
- 用户可以查看并调整各层级策略。

### Task 19：人格 Profile 与学习模式最小闭环

**覆盖**：PRD V2 第 7.5 节

**依赖**：Task 18。

**工作内容**：

- 将人格 Profile 与学习模式分开建模：前者控制表达风格，后者是可执行教学协议。
- 实现最小内置人格和学习模式，不引入社区共享或市场。
- 学习模式不扩展模型能力、工具权限或事实写入权。
- 运行时记录当前人格、学习模式和实际配置来源。

**完成标准**：

- 人格、学习模式、模型策略和工具权限可独立变化；
- 学习模式作为协议执行，不是一句 Prompt；
- 不做社区人格、学习模式或知识包市场。

### Task 20：知识包与能力插件治理最小边界

**覆盖**：PRD V2 第 3.5、8.5 节

**依赖**：Task 19。

**工作内容**：

- 建立知识包版本、来源、适用范围、限制、争议和未知的审查模型。
- 建立能力插件的显式登记、权限、沙箱、预算、失败路径和审计边界。
- 知识包只声明领域数据，不包含任意执行代码。
- 能力插件由统一 Agent Runtime 装配和授权，不创建另一套运行模式。
- 不引入 Web Search、开放网络 Agent、社区市场或通用插件平台。

**完成标准**：

- 知识包和插件职责分离；
- 版本、来源、权限、审计和失败路径可测试；
- 包外 AI 推断只能作为候选假设，不能升级为事实或硬前置。

### Task 21：统一工作台与 PRD V2 整体验收

**覆盖**：PRD V2 第 4、7、8、10、11.6 节

**依赖**：Task 20。

**工作内容**：

- 整合目标、计划、模块、行动、委托、会话、产出、证据、成果、路径和教练入口。
- 默认工作台展示当前状态和下一步，而不是内部数据结构。
- 保持任务学习环境、复核、成果、路径和教练之间的连续用户旅程。
- 建立 PRD V2 整体验收记录，区分已实现、已验证、未实现和暂缓能力。
- 不把完成 Task 17 时的状态夸大为完整 PRD V2。

**完成标准**：

- 用户无需理解旧模型与新模型并存即可完成长期学习闭环；
- 权限、证据、版本、撤销、审计和隐私约束在整体验收中复测；
- 只有本任务通过后，才可另行评估是否达到完整 PRD V2 目标。

## 暂不进入本计划的事项

- 不实现 `CAP-DEFER-001`：云同步、社区市场、跨设备能力及 PRD 第 13 节其余暂缓项目；
- 不修改或应用 `001` 至 `010` 迁移；
- 不自动迁移任何数据库；Task 14 仅在用户明确授权后升级生产学习库，且不得把 `011+` 应用到默认主库；
- 不建立旧模型兼容层、双写、旧入口适配或历史字段映射；
- Task 0-9 不包含完整路径图、成果图和主动后台教练；Task 15-17 仅实现 `CAP-FUT-001` 的最小可用版本，不代表完整可视化平台或通用主动巡航系统；
- Task 18-21 仅补齐 PRD V2 已确认的最小工作流，不授权社区市场、开放网络 Agent、完整知识库、Embedding 或通用插件平台；
- 除已确认的 Task 9 Python 正则标准包和证据分析 Provider 策略外，后续新增沙箱、插件、Provider、模型策略或最终 UI 文案需单独确认；
- 不把计划文档中的任务描述当作已经实现的功能。

## 交接记录格式

每次实施更新 `docs/progress/nautilus-development-status.md` 时，至少记录：

1. 完成的任务编号和需求编号；
2. 实际修改文件和新增迁移；
3. 测试命令、数据目录和结果；
4. 默认数据库、备份和恢复是否改变；
5. 已知问题、未实现项和精确下一项任务。
